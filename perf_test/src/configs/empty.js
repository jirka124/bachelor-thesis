export const performAuth = async ({ browser }) => {};

export const beforePerformPageTest = async () => {};

export const reportName = "EMPTY";

export const beforeAuthUrls = [];

export const afterAuthUrls = [];

export default {
  beforePerformPageTest,
  performAuth,
  reportName,
  beforeAuthUrls,
  afterAuthUrls,
};
